# Environment variables for Docker Compose

# Backend URL (for frontend to connect to backend)
# When running on separate VMs, set this to your backend VM's IP/domain
BACKEND_URL=http://localhost:5000/api

# Frontend URL (for CORS configuration)
# When running on separate VMs, set this to your frontend VM's IP/domain
FRONTEND_URL=http://localhost:3000

# Example for separate VMs:
# BACKEND_URL=http://192.168.1.100:5000/api
# FRONTEND_URL=http://192.168.1.101:3000

# Or with domain names:
# BACKEND_URL=https://api.yourdomain.com/api
# FRONTEND_URL=https://app.yourdomain.com